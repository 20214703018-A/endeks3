# GEOPROP AI İlan & İmar Toplayıcı Bağımlılıkları
curl_cffi>=0.7.0
beautifulsoup4>=4.12.0
pyarrow>=18.0.0
duckdb>=1.1.0
shapely>=2.0
# Veri toplayıcılar / motorlar (isteğe bağlı; ilgili modül kullanılmıyorsa gerekmez)
xlrd>=2.0            # TÜİK .xls tabloları
pypdf>=4.0           # YÖK T102, URAP PDF
pymupdf>=1.24        # Resmî Gazete karar PDF → görüntü (OCR için; ayrıca sistemde tesseract + tur dili)
rasterio>=1.4        # Sentinel-2 pencere okuma (uydu değişimi)
boto3>=1.34          # Copernicus Data Space S3 oturumu
numpy>=1.26
pandas>=2.0.0
openpyxl>=3.0.0
